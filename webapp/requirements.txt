flask>=3.0
gunicorn>=21.0
psycopg[binary]>=3.1
hyperbrowser>=0.1   # optional: only used if HYPERBROWSER_API_KEY is set (see reconcile.py)
tzdata             # zoneinfo needs this on slim/Windows images or IST silently falls back to UTC
shapely>=2.0        # geometry diffing (geodiff.py) — polygon equality/area/intersection
